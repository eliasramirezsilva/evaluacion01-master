import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'evaluacion01',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
